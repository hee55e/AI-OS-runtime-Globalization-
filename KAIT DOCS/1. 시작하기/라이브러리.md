# 라이브러리 안내

> 코드 동기 기준: 2026-02-03
> 원칙: 문서보다 코드가 우선입니다.

## 현재 구현
- KAIT는 별도 라이브러리 카탈로그 UI보다, 기능별 Direct/Handler/UTCP manifest를 통해 모듈을 구성합니다.
- 설치형 의존성보다는 Provider API 키 등록과 실행 설정이 사용성에 직접 영향을 줍니다.

## 프론트엔드 연결
- 설정 화면(Provider/Env): `kait_os/kait_studio/src/components/settings/UserSettingsModal.tsx`

## 백엔드 연결
- Provider 스키마 조회: `kait_os/routers/system/provider_schemas.py`
- 사용자 환경변수 관리: `kait_os/routers/system/user_env.py`

## TODO
- [ ] 라이브러리/플러그인 카탈로그 화면을 KAIT_STUDIO에 통합
- [ ] 설치/활성/권한 상태를 한 화면에서 비교하는 관리자 뷰 추가

## 관련 코드
- `kait_os/routers/system/provider_schemas.py`
- `kait_os/routers/system/user_env.py`
